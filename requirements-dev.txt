# 开发和测试依赖 - ASIN数据推送工具
# 安装命令：pip install -r requirements-dev.txt

# ==================== 先安装核心依赖 ====================
-r requirements.txt

# ==================== 开发依赖 ====================

# 代码测试框架
pytest>=7.0.0,<8.0.0
pytest-cov>=4.0.0,<5.0.0
pytest-mock>=3.10.0,<4.0.0

# 代码格式化
black>=23.0.0,<24.0.0
isort>=5.12.0,<6.0.0

# 代码检查
flake8>=6.0.0,<7.0.0
pylint>=2.17.0,<3.0.0

# 类型检查
mypy>=1.5.0,<2.0.0

# 文档生成
Sphinx>=7.0.0,<8.0.0
sphinx-rtd-theme>=1.3.0,<2.0.0

# 性能分析
memory-profiler>=0.60.0,<1.0.0
line-profiler>=4.0.0,<5.0.0

# 安全检查
bandit>=1.7.0,<2.0.0
safety>=2.3.0,<3.0.0

# 构建和打包
build>=0.10.0,<1.0.0
twine>=4.0.0,<5.0.0

# ==================== 使用说明 ====================

# 开发环境设置：
# 1. pip install -r requirements-dev.txt
# 2. pre-commit install (如果使用pre-commit)

# 代码质量检查：
# - 格式化: black . && isort .
# - 检查: flake8 . && pylint *.py
# - 类型检查: mypy .
# - 安全检查: bandit -r . && safety check

# 测试运行：
# - 基础测试: pytest
# - 覆盖率测试: pytest --cov=.
# - 详细测试: pytest -v --cov=. --cov-report=html 